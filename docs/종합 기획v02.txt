피드백
(V) 1. 초기 화면 수업반 선택지 옆에 수업반 스크립트추가
(V) 2. 공강인 경우 선택지 추가 필요
    => 예상되는 버그: 만약에 공강을 선택해야 하는데 선택힐 수 있는 과목이 하나밖에 없으면 문제가 생길수도
(V) 3. 깃허브 서버 404 에러 픽스
(V) 4. 마지막 화면(시간표 완성)에서 시간표 만들기 버튼을 초기 화면으로 돌아가는 버튼으로 수정
(X) 5. 이전 선택지로 돌아가는 버튼 추가
(V) 6. 프린트 선택시 사용 용이하게 크기 조정(명찰정도)
    => 두가지 크기 한번에 출력되게 함
(*) 7. 임의로 설정된 과목 명칭 명확하게 수정 => 데이터 수정 필요

()8. 핸드폰에서 이미지로 저장
    => 적당한 테이블을 만들어서 캡쳐하게 하면 될듯
    => 핸드폰 크기에 적당하게 과목 이름을 줄여야 함 (예: 자율(자치~) -> 자율)
    => 작은 표 출력에도 쓸 수 있음
